# Phase registry

A flat, at-a-glance list of every completed phase - number, title, commit
(where known), status, and a one-line note. Manually maintained, kept in
sync with `docs/roadmap.md`'s "Completed phases" section (that document
has the full write-up per phase; this one is just the index). See
`docs/roadmap.md`'s "Roadmap numbering policy" for how numbers are
assigned and why this file exists.

Not a database, not generated - a plain text table, updated by hand
whenever a phase completes.

| # | Title | Commit | Status | Notes |
|---|---|---|---|---|
| 0/1 | Foundation | - | complete | CLI, schemas, local mesh validation/preview, read-only slicer discovery, project scaffolding. |
| 2 | CAD generation helpers | - | complete | `factory generate-openscad`, 4 templates. |
| 3 | Manufacturing knowledge & printer-aware planning | - | complete | `config/manufacturing/`, decision engine. |
| 4 | Human manufacturing decision workflow + product vision foundations | - | complete | `list-options`/`choose-option`; `docs/product-vision.md`. |
| 5 | Manufacturing knowledge maintenance | - | complete | Read-only knowledge-base inspection commands. |
| 6 | Visual preview package foundation | - | complete | `preview-index`/`preview-project`. |
| 7 | CAD backend routing & CadQuery starter | - | complete | `route-cad`, `generate-cadquery --template mechanical-plate`. |
| 8 | Local visual preview board foundation | - | complete | `preview-board`. |
| 9 | Local render coverage and multi-part preview improvements | - | complete | `render-coverage`, `plan-renders`. |
| 10 | Preview board action suggestions | - | complete | `suggested_actions` on board cards. |
| 11 | Preview board health signals | - | complete | `health_signals` rollup. |
| 12 | Local review gate command | - | complete | `factory review-gate`. |
| 13 | Shared project inspection refactor | - | complete | `factory/project_inspection.py` extracted. |
| 14 | Local example project library foundation | 9de3c3c | complete | `examples/simple-nameplate`, `examples/mechanical-plate`, `examples/future-organic-models/`. |
| 15 | Multipart example project | 9808dcc | complete | `examples/multipart-classroom-sign/`. |
| 16 | Meshy approval/cost gate design | 469e697 | complete | `docs/meshy-approval-gate.md`, `config/future_cloud_tools.json`, `factory check-future-tools`. Design only - no Meshy implementation. |
| 17 | Fix example test side effect | a1b1116 | complete | Stopped `preview-project` tests from mutating committed `examples/`. |
| 18 | Guard tests from mutating committed examples | a5d02c3 | complete | `tests/test_examples_write_safety.py` static guard. |
| 19 | Storage bin lid example project | c1895ad | complete | `examples/storage-bin-lid/`. |
| 20 | Roadmap numbering and phase registry cleanup | 5ed4f1f | complete | This document; roadmap numbering policy; future tracks unnumbered. |
| 21 | Blender local track planning scaffold | 1382d57 | complete | `docs/blender-local-track.md`, `config/future_local_tools.json`, `factory check-local-tools`. Planning only - no Blender implementation. |
| 22 | Connect design quality standard to future gates | a586924 | complete | Cross-references `docs/design-quality-standard.md` into `docs/meshy-approval-gate.md` and `docs/blender-local-track.md`. Docs only - no implementation. |
| 23 | Human review quality checklist | deb28fb | complete | `docs/review-gate.md`'s "Human review quality checklist"; matching updates to `docs/slicer-review-workflow.md`, `docs/preview-board.md`, `docs/visual-preview-package.md`, `README.md`. Docs only - `review-gate` behavior unchanged. |
| 24 | Design intent brief schema planning | 2826680 | complete | `docs/design-intent-brief.md`; additive `design_intent` shape; two concept examples illustrate it. Docs/planning only - no schema or product code changed. |
| 25 | Design intent manufacturability check | 1ee322a | complete | `factory/design_intent_check.py`, `factory check-design-intent`. Read-only advisory `max_size_mm` vs. known printer build volumes; no approval/print-readiness behavior. |
| 26 | Design intent visibility in project reports | 8e3efaf | complete | `summarize_design_intent()`; `factory report`'s `Design Intent:` section; `design_intent_summary` field on preview-board project entries. Visibility only - `review-gate` unchanged, no approval/scoring/print-readiness behavior. |
| 27 | Design intent preview board visualization | ca8df64 | complete | `describe_design_intent_for_board()`; `design_intent_detail` field on preview-board project entries; a per-project "Design Intent" HTML card (design intent, manufacturing overview, artifact/review badges). Visualization only - JSON/report compatibility preserved, no approval/scoring/print-readiness behavior. |
| 28 | Source discovery and reference board planning | 94f3176 | complete | `factory/reference_board.py`; `reference_board_summary` field on preview-board project entries; a compact "Reference Board" HTML card. Planning/data-model only - no web crawling, scraping, search, downloading, or API integration; every `source_url` is inert metadata, never fetched. |
| 29 | Reference Board CLI management | 2ed579f | complete | `factory reference-board init/show/validate/add/list`; `init_reference_board()`/`add_reference()`/`normalize_references()` in `factory/reference_board.py`. Local-only CLI on top of Phase 28's model - no search/scraping/downloading/API calls; advisory validation never fails on incomplete data, only on malformed JSON. |
| 30 | Intelligent project intake engine | 17e40f9 | complete | `factory/project_intake.py`; `factory intake analyze`; `intake_summary` field on preview-board project entries; a "Project Intake" HTML card; `examples/intake-benchmarks/teacher-nameplate.md`. Fully deterministic keyword/regex heuristics only - no AI, no LLM, no network, no CAD generation. |
| 31 | Intake-to-brief draft generation | 270acb9 | complete | `factory/brief_generator.py`; `factory intake suggest-brief [--json] [--write] [--force]`; `draft_brief_summary` field on preview-board project entries; a "Draft Brief" HTML card. Confidence-gated draft generation only - never re-parses free text, never writes without explicit `--write`, never overwrites an existing `brief.json` without `--force`. |
| 32 | Brief update / merge workflow | 1c3c2a3 | complete | `merge_draft_brief()`/`apply_merge()`/`write_merged_brief()` in `factory/brief_generator.py`; `factory intake suggest-brief --update [--write]`; `brief_update_summary` field on preview-board project entries; a compact "Brief Update" HTML card. Safe merge only - never overwrites existing human-authored content, `--force`/`--update` mutually exclusive, malformed existing brief refused rather than guessed. |
| 33 | Project readiness dashboard & design orchestrator | (this phase) | complete | `factory/design_orchestrator.py`; `factory readiness [--json]`; `design_orchestrator_summary` field on preview-board project entries; a "Project Readiness" HTML dashboard (summarizes, doesn't replace, existing cards). Deterministic readiness scoring + engine recommendation only - no CAD generation, no engine execution, reuses `factory.router.recommend_tool()` rather than a second keyword table. |
| 34 | Readiness-Gated CAD Generation Router | 724a81d | complete | `factory/generation_gate.py`; `factory generate-from-readiness [--confirm-generate] [--json]`; `generation_gate_summary`/`generation_execution_summary` fields on preview-board project entries; a "Generation Gate" HTML card section (Decision/Engine/Ready/Reason/Receipt available/Last execution). Execution receipts (`generated/generation_receipt.json`, confirmed runs only) and normalized artifact tracking, reusing existing OpenSCAD/CadQuery generators and manifest/validation infrastructure rather than duplicating them - no CAD backend of its own, no Blender/Meshy/FreeCAD execution. |
| 35 | Guided Export, Validation, Preview, and Artifact Finalization | 266796c | complete | `factory/export_pipeline.py`; `factory export-from-cad [--confirm-export] [--json] [--source ...] [--output-dir ...] [--overwrite-stl] [--validate] [--render] [--all] [--resume]`; `export_pipeline_summary` field on preview-board project entries; a "Post-Generation Pipeline" HTML card section. First automated subprocess execution in this repo (OpenSCAD CLI export only, argument-list, timeout-bounded, full post-exit verification) - CadQuery source remains manual-only by existing policy. Execution receipts (`generated/export_receipt.json`, upserted per source file, a sibling of Phase 34's receipt) and an artifact registry, reusing the existing mesh validator/renderer/manifest checks rather than duplicating them. |
| 36 | Slicer Review Readiness Promotion and Review Package | aa54f53 | complete | `factory/slicer_readiness.py`; `factory slicer-readiness <project> [--json] [--create-package] [--confirm-package] [--output-dir ...] [--approve] [--approval-note ...] [--refresh] [--include-warnings] [--force-package]`; `slicer_readiness_summary` field on preview-board project entries (wired via `preview_board.gather_board_data()`, not `project_inspection.summarize_project()` - see "Architectural note" in `docs/slicer-readiness.md`); a "Slicer Review Readiness" HTML card section. A deterministic, weighted (never blocker-overriding) readiness score; a separate human-approval lifecycle with fingerprint-based invalidation; a local review package (`slicer_review/slicer_review_manifest.json`, conforming to the pre-existing `schemas/slicer_review.schema.json`) referencing existing STL/validation/render artifacts rather than copying them. Reuses `factory.review_gate`, `factory.export_pipeline`'s receipts, `factory.manufacturing.manifest`, and `factory.slicer.local_slicer_probe` rather than duplicating any of them - never slices, uploads, queues, or prints anything; `auto_print_allowed` is always `false`. |
| 37 | Slicer Profile Inspection & Manual Review Workspace | b73c6e9 | complete | `factory/manual_review_workspace.py`; `factory review-workspace <project> [--json] [--create-workspace] [--confirm-workspace] [--output-dir ...] [--force-workspace]`; `manual_review_summary` field on preview-board project entries (wired via `preview_board.gather_board_data()`, same architectural pattern as Phase 36 - see "Architectural note" in `docs/manual-review-workspace.md`); a "Manual Review Workspace" HTML card section. Organizes everything a human needs before opening a slicer on top of Phase 36's already-computed readiness: local printer/material profile inspection (never inventing an unknown value, e.g. layer height, which this repo's knowledge base never records), a structured multi-category review checklist, and a deterministic review_confidence/remaining_risk pair. Reuses `factory.slicer_readiness.assess_slicer_readiness()` and `factory.manufacturing.knowledge` rather than duplicating either - never slices, generates G-code, or prints anything. |
| 38 | Slicer Review Intelligence & Print Risk Analysis | 23cf26c | complete | `factory/slicer_intelligence.py`; `factory slicer-inspect <project> [--json]` (entirely read-only, no write flag); `slicer_intelligence_summary` field on preview-board project entries (wired via `preview_board.gather_board_data()`, same architectural pattern as Phase 36/37 - see "Architectural note" in `docs/slicer-intelligence.md`); a "Slicer Intelligence" HTML card section. Deterministic build-volume-fit analysis (a new per-axis remaining-margin calculation atop the existing `check_build_volume_fit()`), geometry risk detection (Tall Narrow Geometry, Large Flat Areas, Thin Features, Fragile Features, Multi-part Alignment - only categories supported by already-measured mesh_stats), and generic never-computed support/orientation/adhesion/multi-material review prompts. A deterministic risk_level/confidence pair, purely informational and never blocking. Reuses `factory.manual_review_workspace.assess_manual_review_workspace()`, each STL's already-written validation report, and `factory.validators.dimension_check.check_build_volume_fit()` rather than duplicating any of them - never slices, generates G-code, or contacts a printer. |
| 39 | Slicer-Aware Review Profiles & Analysis History | 82f5ca4 | complete | `factory/slicer_profiles.py`; `factory/slicer_history.py`; `factory slicer-inspect <project> [--json] [--history] [--compare] [--save-analysis]`; `slicer_history_summary` field on preview-board project entries (wired via `preview_board.gather_board_data()`, same architectural pattern as Phase 36-38 - see `docs/slicer-analysis-history.md`); the existing "Slicer Intelligence" HTML card extended (not replaced) with a detected slicer profile name and a compact analysis-history addendum. Slicer-aware review profiles (Bambu Studio/OrcaSlicer/PrusaSlicer/Unknown, never inventing an installed profile - `local_slicer_probe.py` extended in place with a PrusaSlicer candidate rather than a second registry) extend `evaluate_slicer_intelligence()`'s existing checklist additively. A lightweight, append-only, explicit-only analysis history (`generated/slicer_analysis_history.json`, only ever written by `--save-analysis`) with live-vs-last-saved change comparison across 8 categories - history is observational only, never affecting readiness, approval, slicing, or printing. |
| 40 | Unified Project Timeline & Event Model | aba6b4b | complete | `factory/project_timeline.py`; `brief.json["status_history"]` (additive, append-only, never retroactive - `factory.project_store.advance_status()`); `factory timeline <project> [--json]` (entirely read-only, no `--history` flag - removed as redundant during roadmap review); `timeline_summary` field on preview-board project entries (wired via `preview_board.gather_board_data()`, same architectural pattern as Phase 36-39); the existing card set gained a "Project Timeline" card. The Factory's project memory: a chronological event log derived entirely from existing receipts (generation/export/slicer-readiness/manual-review-workspace/slicer-analysis-history) - stores nothing new for derived events, never a second receipt/history format. A stage a project has reached but has no recorded timestamp for is reported `"unavailable"`, never silently omitted. Formalized the "Aggregation Layer Convention" in `docs/architecture.md` as a standing rule. Merged the conceptual foundation with the (renumbered) Phase 41 artifact-history work per an explicit roadmap-revision approval; Phase 47 moved earlier in the roadmap ahead of the organic-modeling phases. |
| 41 | Artifact History & Diff Planning | aba6b4b | complete | `factory/artifact_history.py`; `factory artifact-history <project> [--json]`, `factory artifact-diff <project> --from V --to V [--json]`, `factory artifact-rollback-plan <project> --to V [--json]` (all three entirely read-only, no write flag anywhere); `artifact_history_summary` field on preview-board project entries (wired via `preview_board.gather_board_data()`, same architectural pattern as Phase 36-40); the existing card set gained an "Artifact History" card. A derived, read-only artifact version history and diff/rollback-planning system built directly on Phase 40's timeline - version numbers are the 1-based ordinal of each artifact-relevant timeline event, never a stored counter. Diffing reuses Phase 40's own already-detected material/printer/risk/warning-change events (by timestamp range) rather than re-deriving change detection. Rollback planning is a report only - never restores, copies, or deletes a file, and never modifies a manifest; actual restoration remains a future, separately-approved capability. |
| 42 | Project Health Dashboard & Unified Factory Status View | 5ea688f | complete | `factory/project_health.py`; `factory health <project> [--json] [--verbose]` (entirely read-only, no write flag); `project_health_summary` field on preview-board project entries (wired via `preview_board.gather_board_data()`, same architectural pattern as Phase 36-41); the existing card set gained a "Project Health" card - the first card on every project. A deterministic, weighted health score (7 documented categories) that never overrides a blocker - a project can score 85% and still report `Status: Blocked`. A 12-stage lifecycle derived from existing evidence (brief status + the receipt-backed slicer-readiness stack), carefully distinguishing a genuine obstruction from a normal not-yet-reached state. Aggregated blockers/warnings/risks read verbatim from the modules that produced them (never rewritten); a small set of new, deterministic "strengths" signals; a single next-action recommendation preferring each layer's own already-computed text. Reuses `factory.project_inspection`/`factory.slicer_readiness`/`factory.manual_review_workspace`/`factory.slicer_intelligence`/`factory.project_timeline`/`factory.artifact_history` rather than duplicating any of them - creates no new manufacturing logic, no new readiness rules, and no write path of its own. |
| 43 | Design, Manufacturing, Slicer & Tool Registry | e6d573b | complete | `factory/engine_registry.py`; `factory engines [--json]` / `factory engines probe [--json]`; a board-wide (not per-project) "Tool Environment" `preview-board` section and a purely additive `tool_environment_summary` field on `factory health`'s output (never touches `health_score`). Canonical, permanent registry for 13 tools (OpenSCAD stable/snapshot, CadQuery, Blender, FreeCAD, Meshy, Plasticity, Autodesk Fusion, Onshape, Bambu Studio, OrcaSlicer, PrusaSlicer, Bambu Connect) - reuses `factory.cad.backend.is_cadquery_available()`, `factory.export_pipeline.resolve_openscad_executable()`, `factory.slicer.local_slicer_probe.probe_slicers()`, and `factory.future_local_tools`/`factory.future_cloud_tools` rather than duplicating any of them. Architecture and read-only discovery only - detection (path/PATH/package-metadata checks, plus one bounded `openscad --version` call) is never qualification, and qualification is never execution approval; every tool's `qualification_status` stays `not_tested`/`not_applicable`/`not_installed` (formal qualification testing is Phase 44). Never installs, upgrades, or launches a GUI application; never executes Blender/FreeCAD/a slicer/Meshy; never contacts a printer, cloud service, or Homebrew; `automatic_print_permission` is always `false`. |
| 44 | Local Tool Environment Qualification | 90cf8b8 | complete | `factory/tool_qualification.py`; `factory engines qualify [<tool_id>] [--json] [--verbose]` (entirely read-only w.r.t. the repo; temporary fixtures only, always in a `tempfile.TemporaryDirectory()`, never persisted). Evidence-based qualification for the 8 in-scope tools (OpenSCAD stable/snapshot, CadQuery, Blender, FreeCAD, Bambu Studio, OrcaSlicer, PrusaSlicer); the 5 deferred tools (Meshy, Plasticity, Autodesk Fusion, Onshape, Bambu Connect) get `qualification_status: "unsupported"` regardless of local detection. Reuses `factory.engine_registry.probe_all_tools()`/`get_tool_registry()` and `factory.validators.mesh_validate.validate_mesh()` rather than duplicating either. OpenSCAD (stable) is the only tool qualified end-to-end (`factory_workflow_verified`: a fixed cube fixture, one bounded argument-list/`shell=False`/timeout-bounded `openscad` export subprocess call, then the existing Factory validator) - real local result on this machine: `qualified`. CadQuery gets an equivalent in-process capability test when installed (never "arbitrary project Python" - fixed qualification code only, calling the already-installed `cadquery` library's own API). Blender and FreeCAD are never passed to `subprocess` at all (docs/blender-local-track.md's standing rule, extended to FreeCAD); both, plus all three slicers, stop at `metadata_only`/`requires_manual_qualification` if detected. `execution_approved` is hardcoded `false` on every result, with no code path that ever sets it `true` - qualification is evidence for a later approval decision (Phase 45+), never that decision itself. Preview Board gained one text line (no new section, no automatic qualification during board generation); `factory.project_health`'s `health_score` is entirely untouched. |
| 45 | Blender Local Execution Gate & Adapter | 35b9f42 | complete | `factory/blender_gate.py` (read-only permission/readiness/dry-run planning, zero subprocess calls) and `factory/blender_adapter.py` (the only module in this repo that ever passes Blender to `subprocess`); `factory blender inspect [--json]` / `factory blender qualify [--confirm-fixture] [--json] [--verbose]`. Locked progression Detected -> Metadata Qualified -> Headless Runtime Qualified -> Fixture Execution Qualified -> Adapter Qualified -> Project Execution Eligible -> Explicit Human Confirmation -> Actual Project Execution - this phase reaches, at most, Adapter Qualified, for one narrow `fixture_organic_model` workflow only; `project_execution_approved` is hardcoded `false` on every result. Exactly two real Blender invocations exist: a bounded, Python-free `--version` headless probe (always) and, only with explicit `--confirm-fixture` confirmation, one `--python blender_fixtures/factory_qualification_fixture.py` run (a fixed, hand-written, repository-reviewed sphere fixture living outside `src/` so its `import bpy` never collides with the pre-existing repo-wide no-Blender-execution-code scan) exported to a temporary STL, validated by the existing `factory.validators.mesh_validate.validate_mesh()` and previewed by the existing `factory.previews.render_preview.render_preview()` - no Blender-specific validator or visual-QA subsystem. Every invocation: absolute already-resolved binary path, argument list, `shell=False`, a hard timeout, `--background`/`--factory-startup`/`-Y`/`--offline-mode`. Real local result on this machine: Blender detected (v5.2.0), headless runtime verified, full fixture pipeline `qualified` (STL export pass, Factory validation WARN, Factory preview PASS, cleanup verified). `factory.tool_qualification`'s own Blender result (Phase 44), `factory.engine_registry`'s static Blender record, `factory.project_health`'s `health_score`, `config/future_local_tools.json`'s `blender.enabled`/`requires_explicit_human_approval`, and `factory.cad.backend`'s `blender` status all stay unchanged. Never installs, upgrades, or GUI-launches Blender; never installs an add-on; never contacts a slicer, printer, or network; never generates G-code; never writes to `examples/` or `projects/`. |

| 46 | Meshy Cloud / Cost / License / Privacy Approval Gate | 96015cf | complete | `factory/meshy_approval.py`; `factory meshy status/policy/approval-status [--json]` (fully read-only) plus `factory meshy approve-policy --ack-cost --ack-license --ack-privacy --ack-provenance [--approved-by NAME]` / `factory meshy revoke-policy [--reason TEXT]` (the only two write paths, both local-file-only, both requiring explicit acknowledgement of every policy area). Turns the Phase 16 planning scaffold (`docs/meshy-approval-gate.md`) into a concrete, machine-readable policy/approval model: a committed `config/meshy_policy.json` (cost cap/credit-cap structure, license/commercial-use posture, always-`false` `approval.execution_enabled`) joined at runtime with Phase 43's registry record and Phase 16's `config/future_cloud_tools.json` kill switch - neither rewritten. Deterministic `gate_status` (`needs_cost_policy` -> `needs_license_policy` -> `needs_human_approval` -> `approved_for_future_api_integration`, or `revoked`) and `evaluate_meshy_phase47_readiness()`, whose `ready_for_phase47=True` means only "the policy scaffold Phase 47 needs now exists," never "safe to call Meshy." A privacy/data-class policy (student/private/classroom data forbidden for cloud upload by default) and a `factory.reference_board` license-to-cloud-upload-permission mapping (both pure classification functions - `reference_board.json` is never scanned or mutated). Preview Board gained one compact global line (`meshy_policy_summary`, a field deliberately separate from `tool_environment_summary` so `factory.project_health` stays untouched); no per-project card, no automatic qualification. Zero Meshy network calls, zero credential reads (this module doesn't even check whether a Meshy API key env var exists), zero paid API calls, zero data upload - proven by tests that monkeypatch `socket.socket`/`subprocess.run`/`os.system` to raise and assert every command still succeeds. |

| 47A | Mocked Meshy Adapter & API Contract | 70785a8 | complete | `factory/meshy_models.py` (pure request/response/task-state data, grounded in `docs/meshy-current-research.md`'s researched public Meshy API contract - no I/O); `factory/meshy_mock_transport.py` (the `MeshyTransport` interface + `MockMeshyTransport`, the only implementation - deliberately no `HttpMeshyTransport`, not even a skeleton); `factory/meshy_adapter.py` (policy/budget gating, mocked task lifecycle, provenance, receipt); `factory meshy plan --prompt TEXT [--json]` (read-only dry-run) / `factory meshy mock-run --prompt TEXT --confirm-mock [--scenario ...] [--project PATH] [--json]` (the mocked execution, gated behind explicit confirmation). Locked progression Policy Approved -> Mocked Adapter -> Mocked Request Planning -> Mocked Task Lifecycle -> Mocked Artifact Retrieval -> Factory Validation -> Factory Preview -> Human Review -> 47A Review -> Separate 47B Approval -> One Controlled Real API Call - this phase reaches, at most, Factory Validation/Preview, for Text-to-3D only; `live_execution_allowed` is hardcoded `false` on every result, and `config/future_cloud_tools.json`'s Meshy kill switch stays untouched. Reuses `factory.meshy_approval.evaluate_meshy_gate()` (Phase 46, never re-implemented), `factory.validators.mesh_validate.validate_mesh()`, and `factory.previews.render_preview.render_preview()` rather than duplicating any of them - a mocked response's own `meshy_printability_claim: {"printable": true}` field is proven never to bypass real Factory validation. Every mock task id is namespaced `mock-meshy-...`; the mock artifact is a fixed, checked-in synthetic STL (`tests/fixtures/meshy/mock_concept.stl`), never downloaded or Meshy-derived. A receipt is built in memory on every run but persisted to disk only with explicit `--project <dir>` (`generated/meshy/mock_concept.stl` + `generated/meshy_receipt.json`) - never into `examples/` implicitly. No `project_timeline`/`artifact_history` reader was added (documented as a future, separately-scoped addition, since the capability remains entirely mocked). Zero network calls, zero credential reads, zero credits/money spent - proven by tests that monkeypatch `socket.socket`/`subprocess.run`/`subprocess.Popen` to raise and assert every command still succeeds; a fake `MESHY_API_KEY` is proven never to leak into any output. |

| 47B | Live Meshy Transport Implementation, No Live Call | c50a7f4 | complete | `factory/meshy_http_transport.py` (the only module in this repo that may open a real network connection - `HttpMeshyTransport` against the real, hardcoded `https://api.meshy.ai` via `urllib.request`, no new dependency added; `LiveMeshyCredentialProvider`/`MockCredentialProvider`); `factory/meshy_ledger.py` (`SpendLedger`, a persistent, gitignored `state/meshy_spend_ledger.json`, reserve-before-submit, atomic writes, fail-closed on corruption); `factory/meshy_live_approval.py` (one-shot, single-use, short-lived live-call approval records at `state/meshy_live_approvals.json`); `factory/meshy_live_adapter.py` (the gated orchestrator enforcing the locked 9-step live-execution order); `factory meshy live-plan --prompt TEXT [--json]` (fully offline) / `factory meshy approve-live-once --prompt TEXT --max-credits N [--json]` / `factory meshy revoke-live-approval APPROVAL_ID` / `factory meshy live-run --prompt TEXT --confirm-live [--json]` (blocked unless every gate passes). Locked order: parse -> policy check -> budget check -> kill-switch check (BOTH `config/future_cloud_tools.json` and `config/meshy_policy.json`'s raw `approval.execution_enabled` required true) -> one-shot approval check -> `--confirm-live` -> only then read `MESHY_API_KEY` -> only then construct the transport -> only then the one network call, with zero automatic retry. Five dedicated tests each prove the credential provider is never called when one of the first five gates blocks. SSRF-hardened artifact download (host allowlist/blocked-private-ranges, at most one re-validated redirect, byte cap, atomic write, Factory-chosen destination never derived from a remote filename). Real HTTP transport code exists, but zero real Meshy contact occurred in this phase - every test uses a fake opener/fake transport; `config/meshy_policy.json` and `config/future_cloud_tools.json` stay byte-identical; `MESHY_API_KEY` was never read for real. Phase 47B (the first real live call) remains **NOT APPROVED**. |
| 47B.7 | Meshy Post-Flight Review & Artifact Hardening | 14ee811 | complete | Full forensic audit of the first real live Meshy artifact (task `01a07ca5-0c06-7459-b1f4-68f383774fae`, 20 credits, `meshy-7`, Text-to-3D): receipt/ledger/approval cross-verification, secret-leak audit (none found), exact validation-WARN root cause (an unconfigured build-volume-fit check, not a geometry defect - watertight/winding-consistent/clean), and dimension review (~1479x1903x1412mm - not manufacturing-ready at that scale). Fixed real bugs found during the audit: `artifact_fingerprint` was hardcoded `null` despite a real downloaded artifact (now a real `sha256:`); no sanitized artifact-CDN host was ever captured (now recorded via `factory.meshy_http_transport.artifact_url_host()`); no `project_timeline`/`artifact_history` adapter existed for a real Meshy receipt (now wired in, provider epoch-ms timestamps correctly converted to ISO8601); a stray armed one-shot approval survived past the successful call (revoked). Added `factory.meshy_ledger.SpendLedger.reconcile_rejected_before_submission()` (`factory meshy reconcile-ledger-entry`) - an explicit, auditable state transition (never silent) used to reconcile the two pre-submission HTTP-401 ledger entries to `confirmed_zero_rejected_before_submission` (no task_id was ever assigned to either, positively confirming zero credits consumed). Both kill-switch flags (`future_cloud_tools.json`/`meshy_policy.json`) re-disarmed to `false` after the test; policy note updated to record the completed smoke test. No second Meshy API call, no additional credits spent. |
| 48 | Hybrid Design Workflow Manager | 49a51ba | complete | `factory/hybrid_workflow.py` - the adaptation-planning layer between a generative-AI/CAD artifact and manufacturing-ready Factory output (`factory workflow plan <project> [--json]` / `factory workflow assess <file> [--json]`, both read-only, no `workflow execute`). Motivated directly by 47B.7's finding that the first real Meshy artifact was geometrically clean but ~1.9m tall - adds the first scale-plausibility assessment in this repo (`assess_scale()`; `factory.validators.dimension_check` only ever checked fit against a *configured printer's* build volume, a different question) and a coarse, always-human-confirmed manufacturing-intent classifier (`assess_manufacturing_intent()`), both reusing `factory.design_intent_check.summarize_design_intent()` rather than re-parsing `brief.json`. Deterministic tool routing (`route_tools()`) reuses `factory.engine_registry.get_tool_registry()` and `factory.blender_gate.evaluate_blender_execution_gate()` verbatim - never a second engine selector, never re-scoring Blender's readiness. Minimum workflow-type set: `organic_concept_to_print`, `mechanical_part_refinement`, `hybrid_organic_mechanical`, `replacement_part_reconstruction`, `functional_product_design`, `decorative_collectible`, `unknown`. Transforms nothing: `automatic_execution_allowed` is hardcoded `false` on every plan and every non-validation adaptation step; no Blender/CAD/slicer/printer/network code path exists anywhere in the module (proven by an AST scan, not just inspection); no new receipt-writer exists (nothing executes, so nothing is persisted yet). `hybrid_workflow_summary` wired into `factory.preview_board.gather_board_data()` at the aggregation point only, per the standing Aggregation Layer Convention - `project_health`'s score is untouched. See `docs/hybrid-workflow.md`. |
| 49 | Blender Adaptation Execution Gate & Controlled Organic Cleanup Workflow | b3b7389 | complete | New `factory/blender_adaptation.py` - the first real Blender execution against a real project artifact in this repo, narrowly scoped to exactly one workflow (`organic_cleanup_workflow`: import a Meshy/CAD-origin STL, apply one explicit uniform scale factor, export a new child STL - never mesh repair, remeshing, decimation, or smoothing). Reuses rather than duplicates: `factory.hybrid_workflow.assess_scale()` for scale plausibility, `factory.blender_gate.evaluate_blender_execution_gate()`/new `plan_organic_cleanup_execution()` for planning/gating (never a second permission system), and one additional bounded subprocess call in `factory.blender_adapter.run_organic_cleanup_workflow()` (still the only module in this repo that ever passes Blender to `subprocess`) against a second, fixed, hand-written, repository-reviewed script (`blender_fixtures/factory_organic_cleanup_workflow.py`, living outside `src/` for the identical reason the Phase 45 fixture script does). Blender never writes directly into a project directory - it writes to a fresh temp directory first, copied into the project only after every check passes. Every confirmed execution (`factory blender-adapt execute <artifact> --target-max-mm N --confirm`) re-checks Blender detection and re-runs a **fresh, full Phase 45 fixture-qualification proof** on that exact call - nothing cached or trusted from any prior run. `factory blender-adapt plan <artifact> [--target-max-mm N]` is fully read-only. Writes `<project>/generated/blender/adapted/<stem>_adapted.stl` (never overwriting the input artifact) and `generated/blender_adaptation_receipt.json` only on success, both collision-protected; the receipt enters `factory.project_timeline` (one new `blender_adaptation` event category) and `factory.artifact_history` (one new additive path-classification rule) - no second lineage model. The adapted output is re-validated/re-previewed through the existing Factory validator/renderer; a Blender-reported success never overrides a Factory `FAIL`. `config/future_local_tools.json`'s `blender.enabled`/`allows_automation`/`allows_background_execution` all stay `false` - this phase's own dated spec is the narrow, explicit human authorization for exactly this one workflow, the same precedent Phase 45's own spec set for its fixture pipeline, never a standing "automation is on" state. `factory.project_health`'s `health_score` stays untouched; `blender_adaptation_summary` wired into `factory.preview_board.gather_board_data()` at the aggregation point only, no new HTML card; `factory blender-adapt` kept as its own Typer group - `factory workflow` stays planning-only per Phase 48's own documented invariant. See `docs/blender-adaptation.md`. |
| 50 | CAD Augmentation Execution Gate & Organic-Mechanical Hybrid Workflow | (this phase) | complete | New `factory/cad_augmentation.py` - the controlled bridge from an already-adapted organic artifact to a manufacturing-ready hybrid product, narrowly scoped to exactly one workflow (`organic_mechanical_augmentation`: generate a parametric functional-feature part - a coin slot, mounting holes, a base plate - and export it as a new, separate STL, never a boolean-merge with the organic mesh - `factory.validators.multipart_check`'s own standing "separate STLs over a fused mesh" policy is followed exactly). Reuses rather than duplicates: `factory.engine_registry.get_tool_registry()` for CAD routing (the same fixed candidate list/suitability field `factory.hybrid_workflow._mechanical_augmentation_step()` already uses), `factory.export_pipeline.resolve_openscad_executable()`/new `run_scad_source_to_stl()` for the one bounded OpenSCAD execution this phase adds, `factory.validators.mesh_validate.validate_mesh()`/`factory.previews.render_preview.render_preview()` for validation/preview, and `factory.blender_adaptation.read_blender_adaptation_receipt()` to describe organic-component lineage. **CadQuery is never executed** - `docs/cad-backends.md`'s standing, unconditional "does not import or execute the CadQuery source it writes" policy is preserved in full (unlike Blender, no CadQuery doc ever staged a future real-execution phase); FreeCAD has no execution path either (`cli_available=False`). Both remain reported candidate engines only. Critical dimensions (base width/length/height; coin slot width/length/depth once any one is requested) are never guessed - `requires_human_input` lists exactly the missing ones, and `execute` requires them as CLI-required options. `factory cad-augment plan <artifact> [--base-width-mm N] ...` is fully read-only. `factory cad-augment execute ... --confirm` generates the parametric `.scad` source, exports it via the reused bounded subprocess path, validates/previews the *new* output (never trusting a zero exit code alone), and writes `<project>/generated/cad_augmentation/<stem>_feature.{scad,stl}` and `generated/cad_augmentation_receipt.json` only on success - both collision-protected, never overwriting the organic input artifact. The receipt enters `factory.project_timeline` (one new `cad_augmentation` event category) and `factory.artifact_history` (one new additive path-classification rule), extending the proven Meshy -> Blender -> CAD lineage chain verified live against `projects/meshy-live-smoke-test`'s real piggy-bank artifact. `factory.project_health`'s `health_score` stays untouched; `cad_augmentation_summary` wired into `factory.preview_board.gather_board_data()` at the aggregation point only, no new HTML card; `factory cad-augment` kept as its own Typer group, separate from `factory workflow`/`factory blender-adapt`. See `docs/cad-augmentation.md`. |

## Future tracks (not phase-numbered)

See `docs/roadmap.md`'s "Future tracks, not yet phase-numbered" section
for the full write-up of each. Listed here only so this registry shows
what's *not* yet a numbered phase, at a glance:

- Meshy approval/cost-gated implementation track
- Blender local repair/render track
- 3MF packaging experiments track
- Advanced slicer review automation track
- Rich organic examples track
- Custom Design Quality Pipeline
- Mac launcher/dashboard track

## Maintaining this file

When a phase completes: add one row above with its number, title, commit
hash (if this session captured one), `complete`, and a one-line note.
Never reuse or renumber an existing row. When a future track above is
actually started, move it out of "Future tracks" and into the numbered
table with the next available number - not a number that may have been
informally associated with it earlier.
